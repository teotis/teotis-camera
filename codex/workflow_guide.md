### 文件介绍

本文件主要供用户挑选、复制、修改常用命令模板。
默认不作为 Codex 的自动必读上下文，除非当前任务的执行，需要参考线程命令和命令适用场景信息。



### prompt

#### 工程复盘与加固：

```C++
任务请求：
先阅读 codex/documentation.md、codex/implement.md，并按需阅读 codex/prompt.md 与相关 docs/plans/** 文件，理解当前项目真实状态、当前需求边界、已有进展，以及前面已经识别出的架构问题。只有在本轮任务明确涉及预研、用户研究、新功能规划或计划维护时，才阅读 codex/plan.md。
再阅读 OpenCamera 当前核心实现代码，基于现有代码现实与上述 codex 文件要求，执行一次当前阶段内的工程复盘与加固。
本次任务目标不是继续加新 feature，不是跨阶段推进，而是：
复盘当前阶段是否真正闭环
识别影响当前阶段稳定性、可维护性、后续演进能力的高优先级架构问题
对这些问题进行必要的收敛、加固、修正和验证
将高价值结论与结果更新到 codex/documentation.md
本次任务边界：
只在当前阶段内工作，不得跨阶段抢跑；
只做与“工程复盘与加固”强相关的工作，不做无关功能扩展；
不做低价值整理、展示型重构、无收益的大规模命名/格式化；
除非确有必要，不修改 plan.md、prompt.md、implement.md；
若对这些指导文件有优化建议，只记录到 documentation.md，不要直接修改。
复盘重点：
当前阶段主目标是否真正成立，而不是表面能跑；
Session / App Shell / Coordinator / Mode Plugin / Device Adapter 的职责边界是否清晰；
状态主体划分是否合理，是否存在同一语义状态被多层重复持有、重复解释、重复驱动；
是否存在高优先级坏味道：
影子状态机
软循环 / 运行时时序耦合
SessionState 过厚
Mode 状态复制
UI 层旁路驱动内核
God Object 倾向
为了分层而分层，导致状态过度分散
阻碍恢复能力、稳定性治理和后续自动化测试的问题
当前代码结构是否足以支撑下一阶段继续开发；
当前遗留技术债是否已经收敛到可接受范围。
执行要求：
先给出“当前阶段是否已达到需要复盘与加固的条件”的判断；
若已达到，则直接进行复盘与加固；
若尚未完全达到，也要在当前阶段范围内先做最影响阶段完成度的复盘与加固项，不得借此转向下一阶段；
复盘不是只写分析，必须结合代码现实，尽可能完成必要的收敛性改动；
每处理一个高价值问题，都要进行对应验证；
验证失败时，先修复本轮引入的问题，再重新验证；
未命中停止条件前，不因只完成一个 patch、一个点、一次文档更新就停止。
输出要求：
先简短写明：
当前阶段 / milestone
当前阶段是否已达到复盘与加固条件
本轮优先处理的 1~3 个最高价值问题
计划执行的验证
然后直接开始执行，不要停留在泛泛计划说明上；
过程中只保留高价值判断，不写低优先级噪音；
每条高优先级问题尽量结合具体文件 / 类 / 字段 / 调用关系举证；
最后将以下内容高密度更新到 codex/documentation.md：
当前阶段 / milestone
当前阶段进度判断
当前阶段是否已完成工程复盘与加固
本轮识别出的高优先级问题
本轮完成的加固 / 收敛改动
验证结果
当前遗留风险
是否已经具备进入下一阶段的条件
若尚不具备，最优先还缺什么
停止条件：
当前阶段复盘与加固已完成，并且代码现实支持“已具备进入下一阶段条件”，但还没有得到我的跨阶段确认；
出现必须依赖我决策、权限、环境、外部资源或信息补充才能继续推进的情况；
当前阶段范围内，已无明确高价值、可实施、可验证的复盘与加固工作；
继续修改会明显引入大于收益的复杂度、风险或技术债。
现在开始执行。
```

#### 坏味道分析

```Markdown
任务请求：

先阅读 `codex/documentation.md`、`codex/implement.md`，并按需阅读 `codex/prompt.md` 与相关 `docs/plans/**` 文件，理解当前项目目标、阶段和已有进展。只有在本轮诊断明确涉及预研、用户研究、新功能规划或计划维护时，才阅读 `codex/plan.md`。
再阅读 `OpenCamera` 当前核心功能代码，重点分析当前架构中的状态主体划分是否合理。

本次任务只做“架构诊断”，不做功能开发，不做无关代码修改；除 `documentation.md` 外，不修改其他指导文件和业务代码。

诊断重点：
1. 状态主体划分是否清晰，是否存在同一语义状态被多层重复持有、重复解释、重复驱动；
2. Session / App Shell / Coordinator / Mode Plugin / Device Adapter 的职责边界是否清晰；
3. 是否存在高优先级坏味道，如：
   - 职责不清
   - 影子状态机
   - 运行时时序耦合
   - 循环依赖或软循环
   - 状态过厚或过度分散
   - God Object 倾向
   - Mode 状态复制
   - UI 层旁路驱动内核
4. 当前最应该优先收敛的架构问题是什么，为什么；
5. 这些问题若不处理，会如何影响后续阶段推进、恢复能力、稳定性治理和自动化测试。

输出要求：
- 先给出总体结论，再给出高优先级问题列表；
- 只记录高优先级、对当前阶段和后续演进真正有影响的信息；
- 每条问题尽量结合具体文件 / 类 / 关键字段 / 调用关系举证；
- 优先指出“真正影响架构健康度”的问题，不要罗列低价值风格问题；
- 若发现问题本质不是“有没有分层”，而是“状态 owner / 调度 owner 没钉死”，要明确指出。

最后：
将本次诊断结论以高密度、去废话、可供后续决策直接使用的形式更新到 `codex/documentation.md`。
不要把低优先级问题、重复描述、泛泛而谈的架构常识写进去。
```

#### 工作启动

```Markdown
（如果当前已经满足进入下一阶段的条件，那我决策允许进入）
（对于显著依赖底层和硬件的需求能力，可以暂时只完成上层部分，并使用时提示暂不支持，与下层的连接做成通用结构，允许日后快速针对具体设备具体适配）
以我的最新需求为主，先阅读 `codex/documentation.md` 和 `codex/implement.md`，必要时补读 `codex/prompt.md`、`docs/plans/INDEX.md` 与相关专项方案，锁定当前最高优先级未完成闭环。`codex/plan.md` 只在本轮明确需要预研、用户研究、新功能规划或计划维护时阅读。
本次只允许在当前需求/阶段边界内推进，不得跨阶段或跨需求扩散。

未命中停止条件前，不因完成单个 patch、单次测试通过、单次 documentation 更新而停止。
你必须持续重复：
识别当前最高优先级缺口 → 完成该闭环所需全部强相关改动 → 运行规定验证 → 修复本轮引入问题 → 更新，整理  codex/documentation.md → 继续下一闭环。

只有在以下情况之一允许停止并汇报：
1. 当前 stage 的 exit checklist 已全部满足，但尚未获得我授权进入下一 stage；
2. 必须依赖我的决策、权限、外部资源或缺失信息；
3. 当前 stage 的 P0/P1 已清空，剩余仅低价值项；
4. 继续推进会显著增加风险，且无法通过当前 stage 验证收敛。

开始执行前，只用简短几行写出：
- 当前 stage
- 本轮先做的闭环
- 准备运行的验证命令
然后直接开始，不要停在计划说明。
```

#### 工程介绍

```Markdown
任务请求：

先阅读 `codex/documentation.md`、`codex/implement.md`，并按需阅读 `codex/prompt.md` 与相关 `docs/plans/**` 文件，理解 `OpenCamera` 的项目目标、架构方向、当前阶段、已完成能力和当前边界。只有在本轮对外材料需要引用新规划/预研结论时，才阅读 `codex/plan.md`。
再阅读 `OpenCamera` 当前核心代码与关键模块实现，但本次任务的目标不是继续开发，而是为了**向外部人员介绍项目**，整理一份适合对外讲解的项目介绍材料。

本次任务目标：
输出一份面向外部人员的 `OpenCamera` 项目介绍文档，要求能帮助外部人员快速理解：

1. 这个项目是什么；
2. 为什么要做这个项目；
3. 它解决什么问题；
4. 当前架构核心思路是什么；
5. 相比传统相机 App 工程，它的设计亮点是什么；
6. 当前已经做到什么程度；
7. 后续还会往什么方向演进。

受众假设：

* 对 Android / Camera 架构有一定技术背景，但不了解本项目细节；
* 不是项目内部开发者；
* 需要快速抓住项目价值、架构主线、阶段进展和差异化亮点。

内容要求：

1. 输出内容必须以“对外介绍项目”为目标，而不是写成内部开发日志；
2. 允许基于代码和 codex 文件总结架构价值，但不要泄露不适合对外展开的内部实现细节、临时方案、低成熟度中间态、过多脆弱点细节；
3. 可以说明当前项目仍在演进中，但表达方式要专业、克制，不要写成问题清单；
4. 重点突出项目目标、设计理念、核心架构、阶段成果、演进路线；
5. 不要堆砌类名、文件名、函数名，除非它们对讲解主线确有必要；
6. 不要写成纯营销文案，要让技术人员读完后觉得结构清楚、判断可信；
7. 若代码现实与文档描述有偏差，以代码真实状态为准，但表达时优先抽象成项目层面的说明。

输出结构要求：
请整理成一份高质量 markdown 文档，建议包含以下部分：

* 项目概述
* 项目背景与重构动机
* 设计目标
* 核心架构思路
* 关键模块职责划分
* 与传统相机工程写法相比的主要改进
* 当前阶段成果
* 当前边界与后续演进方向
* 一段适合口头讲解的简版总结

风格要求：

* 语言专业、清晰、凝练
* 强调“架构主线”和“工程价值”
* 不写成流水账
* 不写成泛泛而谈的架构八股
* 不要把内部复盘内容原样搬进去
* 对尚未完全落地的能力，要明确区分“已完成”“正在建设”“规划方向”

执行要求：

1. 先基于 codex 文件和代码现实提炼讲解主线；
2. 再组织成对外介绍文档；
3. 若发现某些内容不适合对外展开，主动做抽象化处理；
4. 只输出高价值内容，不堆砌无关细节；
5. 在开始写文档前，先简短列出你准备采用的讲解框架；
6. 然后直接生成完整文档。

文件更新要求：
将结果输出到一个新的 markdown 文件，例如：
`codex/external_intro_opencamera.md`

除新增该介绍文档外，不修改业务代码；如需补充极少量上下文记录，可同步更新 `codex/documentation.md`，但不要把对外介绍文档写进 `documentation.md` 主体。

现在开始执行。
```

#### 功能子项分析

```Markdown
任务请求：

先阅读 `codex/documentation.md`、`codex/implement.md`，并按需阅读 `codex/prompt.md` 与相关 `docs/plans/**` 文件，理解当前项目背景、架构方向和整体上下文。只有在本轮功能介绍需要引用新规划/预研结论时，才阅读 `codex/plan.md`。
再阅读 `OpenCamera` 中与【功能名】直接相关的核心代码、关键调用链、关键状态流转和 UI 交互实现。

本次任务目标不是继续开发，而是为了**向外部人员介绍 OpenCamera 中的某个具体功能**，输出一份适合对外讲解的功能介绍文档。

本次介绍对象是：【功能名】
例如：

* zoom 圆盘
* 相机美颜效果
* LivePhoto
* 水印
* 夜景模式中的某个子能力

输出目标：
请整理一份面向外部技术人员 / 合作方 / 面试官 / 工程同行的功能介绍材料，帮助他们快速理解：

1. 这个功能是什么；
2. 这个功能解决什么用户问题；
3. 用户侧体验是怎样的；
4. 该功能在相机整体架构中的位置是什么；
5. 它的核心实现思路是什么；
6. 它涉及哪些关键模块与职责分工；
7. 该功能实现的主要难点是什么；
8. 为了稳定性、性能、兼容性做了哪些关键设计；
9. 该功能当前的能力边界和可演进方向是什么。

内容要求：

1. 输出内容必须以“对外介绍该功能”为目标，而不是写成内部开发记录；
2. 重点突出该功能的产品价值、交互特征、架构位置、核心机制、难点与工程价值；
3. 不要堆砌无意义类名、函数名、文件名，但若某些模块名对讲解主线非常关键，可以少量引用；
4. 不要泄露不适合对外展开的内部实现细节、低成熟度中间态、临时补丁、脆弱点细节；
5. 若代码现实与文档表述有偏差，以代码真实状态为准，但表达时应抽象成适合外部理解的表述；
6. 不要写成营销文案，也不要写成问题清单；
7. 要明确区分：

   * 用户看到的能力
   * 架构层面的支撑机制
   * 当前已完成的部分
   * 暂不展开或后续可演进的部分

分析重点：

1. 该功能的入口、主要交互和用户感知链路；
2. 该功能依赖哪些核心状态、核心控制链路、核心数据流；
3. 该功能与 Session / Mode / Device / UI / Media Pipeline / 算法层 的关系；
4. 该功能在复杂相机工程里最容易出问题的点是什么；
5. 当前实现里最体现工程价值的设计点是什么；
6. 若要对外介绍，哪些内容值得讲，哪些内容应抽象处理。

输出结构要求：
请整理成一份高质量 markdown 文档，建议包含以下部分：

* 功能概述
* 用户价值与体验目标
* 在相机整体系统中的位置
* 核心实现思路
* 关键模块职责划分
* 关键链路说明
* 实现难点与工程挑战
* 稳定性 / 性能 / 兼容性考虑
* 当前能力边界与后续演进方向
* 一段适合口头讲解的简版总结

风格要求：

* 语言专业、清晰、凝练
* 站在“对外讲解功能”的视角组织内容
* 强调功能价值和工程主线
* 不写成流水账
* 不写成泛泛八股
* 不把内部复盘内容原样搬出去

执行要求：

1. 先根据 codex 文件和代码现实，定位与【功能名】直接相关的模块和关键链路；
2. 再提炼适合对外讲解的主线；
3. 若发现某些内容不适合对外展开，主动抽象化处理；
4. 只输出高价值内容，不堆砌无关细节；
5. 在开始生成完整文档前，先简短列出讲解框架；
6. 然后直接生成完整文档。

文件更新要求：
将结果输出到新的 markdown 文件，例如：
`codex/external_intro_[feature_name].md`

除新增该介绍文档外，不修改业务代码；如需补充极少量说明，可在 `codex/documentation.md` 中附加简短记录，但不要把完整对外介绍文档写进 `documentation.md`。

现在开始执行。
```
