# Full Clear Mode V1 Status

`state.tsv` is the scheduler source of truth. Package agents must update state through:

```bash
bash /Volumes/Extreme_SSD/project/open_camera/docs/plans/full-clear-mode-v1-orchestration/launchers/orchestrate.sh mark-state <package-id> <state>
```

Human-readable package evidence belongs in the matching `status/<package-id>.md` file.

