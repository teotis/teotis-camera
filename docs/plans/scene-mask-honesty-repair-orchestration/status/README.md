# Status Files

Each package executor writes to its own coordinator status file and updates its row in `state.tsv`. Do not edit `INDEX.md` or another package status file.

`state.tsv` is the scheduler source of truth. Markdown files are human-readable evidence packs.

