# Stage 7 Session Test Hang Status

Agents must write only to their assigned status file and their own row in `state.tsv`.
Do not edit `INDEX.md` during execution.

`state.tsv` is the scheduler source of truth. Markdown status is for human evidence.
