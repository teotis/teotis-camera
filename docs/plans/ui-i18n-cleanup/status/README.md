# Status Directory

Runtime status for the UI i18n cleanup orchestration.

- `state.tsv` — machine-readable ledger (scheduler source of truth)
- `events.jsonl` — append-only audit log
- `<package-id>.md` — human-readable package status
- `99-finalize.md` — finalize package status
- `launch-<package-id>.log` — raw launch output
