# Status Files

Each package executor writes to its own status file. Do NOT edit `INDEX.md`.

## How To Use

1. Open `package-status-template.md`.
2. Fill in your assigned `<package-id>.md`.
3. Do not edit other packages' status files.
4. Runtime source files are read-only for this research/design package unless the user separately authorizes implementation.
